apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "defect-api.fullname" . }}-test-connection"
  labels:
    {{- include "defect-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "defect-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
