apiVersion: v1
kind: Service
metadata:
  name: {{ include "defect-api.fullname" . }}
  labels:
    {{- include "defect-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "defect-api.selectorLabels" . | nindent 4 }}
